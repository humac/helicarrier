# Server-side secret (required)
HELICARRIER_SECRET=change-me

# MVP shared client key (must match HELICARRIER_SECRET)
NEXT_PUBLIC_HELICARRIER_SECRET=change-me

# Optional fallback expected by API route
OPENCLAW_AUTH_TOKEN=

# Host OpenClaw directory mount source for docker-compose
OPENCLAW_HOME=$HOME/.openclaw
